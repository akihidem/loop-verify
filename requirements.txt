mcp>=1.28
pytest>=8.0
# Optional key-based backends (the default codex backend needs only the codex CLI):
# openai>=1.0        # for LOOP_VERIFY_BACKEND=openai (needs OPENAI_API_KEY)
# google-genai>=1.0  # for LOOP_VERIFY_BACKEND=gemini (needs GEMINI_API_KEY)
