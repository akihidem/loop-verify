"""loop-verify — paid independent-verification backend for the self-verification loop.

Free loop-kit checks Claude with Claude (shared blind spots). loop-verify supplies
the part loop-kit's own README says it cannot do for free: an independent checker
from a DIFFERENT model lineage. Exposed as a selectable MCP suite (mode A active;
B/C/D declared), gated by per-mode entitlement + metering.
"""

__version__ = "0.1.0"
